import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    marginBottom: 5,
  },
  input: {
    backgroundColor: '#ECEFF1',
    borderRadius: 15,
    padding: 10,
  },
});